#includepath "~/Documents/;%USERPROFILE%Documents";#include "basiljs/bundle/basil.js";function draw() {  var tf = b.selections()[0]; // this makes the selected item now being referred to as var tf. the [0] is because the "selection" always returns an array, and this referes to the first object in an array.     var chars = b.characters(tf.parentStory); // this is identifying the words from tf's whole story. tf.parentStory is acting like us highlighting the whole text.      var fonts = app.fonts;  for( var i = 0; i < chars.length; i++ ) { // for all the words in the words array        var charst = chars[i]; // word = the word currently being looked at     b.typo( charst, "skew", b.random(-20,20)); // change the font size of the word[i] to be the size of frequency    b.typo( charst, "baselineShift", b.random(0,3)); // change the font size of the word[i] to be the size of frequency    b.typo( charst, "pointSize", b.random(10,11)); // change the font size of the word[i] to be the size of frequency      var randomIndex = b.floor( b.random(app.fonts.length) );    var fontName = fonts[randomIndex].fullName;    b.println(fontName);        b.textFont( fonts[randomIndex] );        b.typo( charst, "appliedFont",  fonts[randomIndex] );       }}b.go();